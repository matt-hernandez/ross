import { Padding } from '../../interfaces/common-types/positioning';

export interface BorderContainer {
  padding: Padding;
  strokeWidth: number;
  stroke: string;
  backgroundFill: string;
  borderRadius: number;
}
